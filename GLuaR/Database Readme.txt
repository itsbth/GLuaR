The code.db file contains the code provider database for the CodeProvider class. The sln is set to copy it to the build directory.

If you encounter an error when starting GLuaR and the error is a FileNotFoundException, make sure the code.db file is in the build directory.