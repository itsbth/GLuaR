
local props = ents.FindByClass( "prop_physics" )

for _, prop in ipairs( props ) do

prop:GetPhysicsObject():ApplyForceCenter( Vector( 0, 0, 900 ) )

end local props = ents.FindByClass( "prop_physics" )

for _, prop in ipairs( props ) do

prop:GetPhysicsObject():ApplyForceCenter( Vector( 0, 0, 900 ) )

end local props = ents.FindByClass( "prop_physics" )

for _, prop in ipairs( props ) do

prop:GetPhysicsObject():ApplyForceCenter( Vector( 0, 0, 900 ) )

end local props = ents.FindByClass( "prop_physics" )

for _, prop in ipairs( props ) do

prop:GetPhysicsObject():ApplyForceCenter( Vector( 0, 0, 900 ) )

end 